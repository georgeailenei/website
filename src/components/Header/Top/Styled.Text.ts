import styled from "styled-components";

export const StyledTaglineContainer = styled.div`
  margin-left: 16px;
  margin-top: 32px;
  color: #050505;

  span {
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 1px;
  }
`;

export const StyledTaglineText = styled.h1`
  text-transform: uppercase;
  font-size: 24px;
  margin: 0;
`;

export const StyledSpanWrapper = styled.div`
  margin-top: 8px;
`;

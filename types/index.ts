export interface JsonPlaceHolderData {
  userId: number;
  id: number;
  title: string;
  body: string;
}

export type JsonPlaceHolderDataArray = JsonPlaceHolderData[]; // we can do this also niceeeu
